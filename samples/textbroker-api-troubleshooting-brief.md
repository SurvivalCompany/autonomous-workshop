# Sample Brief — Textbroker Client API Integration Troubleshooting

## Context
Textbroker's own published client FAQ documents specific integration friction: 'We do not have WSDL files available for the API. Unfortunately, it is not compatible at this time. Theoretically, the API should work with C# and .NET; however, many clients have experienced significant difficulties with this integration.'

## What this means for businesses
Any business planning to connect Textbroker's ordering/approval API to a .NET-based CMS or internal tool should expect nonstandard integration work: no WSDL means no auto-generated client stubs, so a hand-rolled HTTP/XML-RPC client is likely required.

## Independent research/documentation offer
Clearline Research & Writing can prepare an independent, plain-text troubleshooting or workaround guide (e.g., outlining known API quirks, suggested manual client approaches, and integration checklists) for businesses facing this specific friction. This is a documentation/research deliverable only -- we do not operate, build, or maintain any live integration, account, or software on a client's behalf.

## Delivery
Text document delivered directly to the requester via their own channel. No email sending, publishing, or account operations performed on the client's behalf.
